[build]
builder = "NIXPACKS"

[deploy]
# Migraciones: reactivar cuando DATABASE_URL esté configurada en el servicio backend
# preDeployCommand = "npm run migrate:all"
startCommand = "node src/app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
